// import {
//
// } from './constants';

const initialState = {
  username: '',
};

const homeReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default homeReducer;
